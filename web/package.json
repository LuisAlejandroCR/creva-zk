{
  "name": "@creva-zk/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/copy-zk-artifacts.mjs --allow-missing && vite",
    "build": "node scripts/copy-zk-artifacts.mjs && tsc -p tsconfig.json --noEmit && vite build",
    "zk:copy": "node scripts/copy-zk-artifacts.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@creva-zk/api": "*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^6.3.5",
    "vite-plugin-wasm": "^3.6.0"
  }
}

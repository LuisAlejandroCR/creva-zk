{
  // api/tsconfig.build.json
  // Build variant of api/tsconfig.json: same settings, minus the test
  // files, so specs never land in the published dist/ output.
  "extends": "./tsconfig.json",
  "exclude": ["dist", "node_modules", "test", "src/**/*.test.ts"]
}

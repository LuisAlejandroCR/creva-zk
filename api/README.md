# `@creva-zk/api`

Declared as an npm workspace in the root `package.json` but not implemented yet — there is no
source in this directory. Its intended role, per a comment in `advisor/src/types.ts`, is to hold
domain types shared across workspaces (starting with the `Tier` the backing circuit derives), which
`advisor` currently defines locally instead of importing.

## Status

Not implemented. Nothing to build, run, or test here yet.
